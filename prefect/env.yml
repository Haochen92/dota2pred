name: prefect-env
channels:
  - defaults
dependencies:
  - python=3.9
  - psycopg2
  - tzdata
  - pip
  - pip:
    - prefect
  # Ensure that pip is installed, but use it for other packages if needed
  # Seems like for prefect, cannot be installed via conda, but only pip. 